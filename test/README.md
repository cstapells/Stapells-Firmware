# Tests

Testing will be added at two levels:

- host-side unit tests for state transitions, configuration validation, identity formatting, topic construction, and module recipes;
- embedded smoke tests for each supported processor family, including boot, health-strip order, Wi-Fi recovery, MQTT last-will/heartbeat, OTA, and return-to-Base.
